# frozen_string_literal: true

valid:
  identity: jane
  code: ABC123
  purpose: sign_in
  expires_at: <%= 15.minutes.from_now %>

expired:
  identity: jane
  code: XYZ789
  purpose: sign_in
  expires_at: <%= 1.hour.ago %>

sign_up:
  identity: jane
  code: DEF456
  purpose: sign_up
  expires_at: <%= 15.minutes.from_now %>
