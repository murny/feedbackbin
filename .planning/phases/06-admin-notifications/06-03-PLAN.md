---
phase: 06-admin-notifications
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/controllers/admin/dashboard_controller.rb
  - app/views/admin/dashboard/show.html.erb
  - app/views/admin/dashboard/_actionable_section.html.erb
  - app/views/admin/dashboard/_actionable_tile.html.erb
  - app/assets/stylesheets/admin-dashboard.css
  - test/controllers/admin/dashboard_controller_test.rb
  - config/locales/en.yml
autonomous: true
requirements: [ADMN-01]
tags: [admin, dashboard, cache, trending, top-voted, ideas-this-week]

must_haves:
  truths:
    - "Admin dashboard renders an 'Actionable' section above the existing stats row with three metric tiles"
    - "Each tile renders up to 5 idea rows (D-03) — Ideas this week, Top voted, Trending"
    - "Trending is defined as 'most votes in the last 7 days' (D-02), scoped to voteable_type='Idea' and Current.account"
    - "All three new metrics are cached via Rails.cache.fetch with 10-minute TTL (D-04) and account-scoped keys account_#{id}/dashboard_<metric>/v1"
    - "Existing stat tiles (total/this-month/users/comments) and recent-ideas / recent-comments lists are unchanged (D-01)"
    - "No new database schema (D-05 — confirms zero migrations in this phase)"
  artifacts:
    - path: "app/controllers/admin/dashboard_controller.rb"
      provides: "Three new @ideas_this_week, @top_voted_ideas, @trending_ideas instance variables fetched via Rails.cache.fetch"
      contains: "account_#{account.id}/dashboard_trending/v1"
    - path: "app/views/admin/dashboard/_actionable_section.html.erb"
      provides: "Section heading + 3-tile grid wrapper"
    - path: "app/views/admin/dashboard/_actionable_tile.html.erb"
      provides: "Single tile partial rendering label + 5-item idea list"
    - path: "app/views/admin/dashboard/show.html.erb"
      provides: "Render of _actionable_section ABOVE existing stats grid (per D-01 + UI-SPEC line 274)"
    - path: "app/assets/stylesheets/admin-dashboard.css"
      provides: "Optional .panel--actionable / .actionable-tile CSS module if existing .panel--stat is reused without modification this may be NEW only if needed"
    - path: "config/locales/en.yml"
      provides: "admin.dashboard.show.actionable.* keys per UI-SPEC lines 97-108"
  key_links:
    - from: "app/controllers/admin/dashboard_controller.rb"
      to: "Rails.cache.fetch"
      via: "Three new cache.fetch blocks, each with account-scoped key + 10-minute TTL + .to_a inside the block"
      pattern: "Rails\\.cache\\.fetch.*dashboard_(trending|top_voted|ideas_this_week)"
    - from: "app/views/admin/dashboard/show.html.erb"
      to: "_actionable_section partial"
      via: "render partial: 'admin/dashboard/actionable_section'"
      pattern: "render.*actionable_section"
---

<objective>
Add the ADMN-01 "Actionable" section to the admin dashboard with three new cached metric tiles: Ideas this week, Top voted, Trending (defined as most votes in the last 7 days per D-02). Each tile renders a 5-item list (D-03), cached for 10 minutes with account-scoped keys (D-04 + T-06-02 mitigation). The new section lands above the existing stats row (D-01); the existing tiles and recent-activity lists stay untouched.

Purpose: ADMN-01 requires the admin dashboard to surface actionable metrics. The existing `Admin::DashboardController#show` provides the cache-fetch pattern (3 existing blocks at app/controllers/admin/dashboard_controller.rb:8-33), the `panel panel--stat` BEM shell (lines 17-39), and the multi-tenant account-scope conventions to extend.

Output: 3 new `Rails.cache.fetch` blocks + 2 new partials + view edit + i18n keys + controller test extension. **No schema changes** — D-05 prohibits new preference tables; ADMN-01 is read-only aggregation.
</objective>

<execution_context>
@/Users/smurnaghan/code/feedbackbin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smurnaghan/code/feedbackbin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-notifications/06-CONTEXT.md
@.planning/phases/06-admin-notifications/06-RESEARCH.md
@.planning/phases/06-admin-notifications/06-PATTERNS.md
@.planning/phases/06-admin-notifications/06-UI-SPEC.md
@.planning/phases/06-admin-notifications/06-VALIDATION.md

# Source-of-truth files (read before writing)
@app/controllers/admin/dashboard_controller.rb
@app/views/admin/dashboard/show.html.erb
@db/schema.rb
@app/models/vote.rb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three cached metric queries to Admin::DashboardController#show</name>
  <files>app/controllers/admin/dashboard_controller.rb, test/controllers/admin/dashboard_controller_test.rb</files>
  <read_first>
    - app/controllers/admin/dashboard_controller.rb:1-36 (full file — three existing Rails.cache.fetch blocks at lines 8-33 are the verbatim pattern to extend; PATTERNS analog lines 316-360)
    - db/schema.rb:377-400 (votes + watches table structure — votes has account_id non-null, voteable_type, created_at; no standalone created_at index but the table is small at v1 seed scale per RESEARCH Focus 1)
    - db/schema.rb:182 (confirm ideas.votes_count counter cache exists for the top_voted ordering per RESEARCH Focus 7)
    - app/models/vote.rb (current scope — for the Trending aggregation; verify `account.votes` association via `Account has_many :votes`)
    - app/models/account.rb (find the `has_many :votes` association)
    - test/controllers/admin/dashboard_controller_test.rb (current state; may not exist — verify and create if missing per RESEARCH Wave 0 Gaps line 1094)
    - .planning/phases/06-admin-notifications/06-RESEARCH.md lines 123-163, 434-468, 962-1005 (Focus 1 + Focus 7 + complete trending query)
    - .planning/phases/06-admin-notifications/06-PATTERNS.md lines 313-364 (verbatim dashboard controller extension pattern)
  </read_first>
  <behavior>
    - `Admin::DashboardController#show` now exposes three new instance variables `@ideas_this_week`, `@top_voted_ideas`, `@trending_ideas`, each an `Array<Idea>` of up to 5 items.
    - Each is wrapped in `Rails.cache.fetch("account_#{account.id}/dashboard_<metric>/v1", expires_in: 10.minutes) { ... }` (account-scoped key per T-06-02 mitigation; `/v1` suffix for future cache busting per RESEARCH Focus 7).
    - `@ideas_this_week`: ideas created in the last 7 days, eager-loaded `(:creator, :board, :status)`, ordered `created_at: :desc`, limited to 5.
    - `@top_voted_ideas`: ideas ordered by `votes_count: :desc, created_at: :desc` (uses the existing counter cache at ideas.votes_count), eager-loaded same, limited to 5.
    - `@trending_ideas`: ideas with most votes in the last 7 days (D-02). Two-step query per RESEARCH Focus 1: first `account.votes.where(voteable_type: "Idea", created_at: 7.days.ago..).group(:voteable_id).order(Arel.sql("COUNT(*) DESC")).limit(5).count` → produces `{idea_id => count}`. Then load `account.ideas.includes(:creator, :board, :status).where(id: ids).index_by(&:id).values_at(*ids).compact` to preserve count-descending order.
    - All cache blocks end with `.to_a` (Pattern 1 critical note at RESEARCH line 668 — caching a Relation re-queries on deserialization).
    - The Trending block returns `[]` early via `next [] if ordered_idea_ids.empty?` to avoid a `WHERE id IN ()` query.
    - Existing instance vars (`@stats`, `@recent_ideas`, `@recent_comments`) are untouched.
  </behavior>
  <action>
    Extend `Admin::DashboardController#show` (NOT replace it) with three new `Rails.cache.fetch` blocks. Place them AFTER the existing `@recent_comments` block (~line 33) and BEFORE the `end` of the method. Each block follows the verbatim shape of the existing `@recent_ideas` block at lines 19-25 (PATTERNS lines 320-326).

    `@ideas_this_week` block: cache key `"account_#{account.id}/dashboard_ideas_this_week/v1"`, expires_in 10.minutes. Inside the block: `account.ideas.includes(:creator, :board, :status).where(created_at: 7.days.ago..).order(created_at: :desc).limit(5).to_a`. Per UI-SPEC line 260 "States Inventory" treat as a 5-item list (D-03), not a counter.

    `@top_voted_ideas` block: cache key `"account_#{account.id}/dashboard_top_voted/v1"`, expires_in 10.minutes. Inside: `account.ideas.includes(:creator, :board, :status).order(votes_count: :desc, created_at: :desc).limit(5).to_a`. The `votes_count: :desc, created_at: :desc` compound ordering matches Phase 4 04-03 roadmap precedent (RESEARCH Focus 7 line 468).

    `@trending_ideas` block: cache key `"account_#{account.id}/dashboard_trending/v1"`, expires_in 10.minutes. Inside the block, two-step computation per RESEARCH Focus 1 / RESEARCH Code Examples lines 986-1003:
      1. `trending_counts = account.votes.where(voteable_type: "Idea", created_at: 7.days.ago..).group(:voteable_id).order(Arel.sql("COUNT(*) DESC")).limit(5).count`
      2. `ordered_idea_ids = trending_counts.keys`
      3. `next [] if ordered_idea_ids.empty?` (early return to avoid empty IN clause)
      4. `by_id = account.ideas.includes(:creator, :board, :status).where(id: ordered_idea_ids).index_by(&:id)`
      5. `ordered_idea_ids.map { |id| by_id[id] }.compact`
    The `Arel.sql("COUNT(*) DESC")` is MANDATORY (Rails 7+ raises `UnknownAttributeReference` on raw `order("COUNT(*) DESC")` per RESEARCH Pitfall 3). The `voteable_type: "Idea"` filter is MANDATORY (Comment is also Voteable per app/models/comment.rb:5; mixing IDs would corrupt the trending list per RESEARCH Pitfall 2).

    Add controller test extensions to `test/controllers/admin/dashboard_controller_test.rb` (create the file if it does not exist; mirror the shape of any existing admin controller test such as `test/controllers/admin/changelogs_controller_test.rb` for auth setup). Tests required (distinct, no `each` bundling):
      - "show assigns @ideas_this_week with up to 5 recent ideas" — sign in as admin, GET admin dashboard, assert `assigns(:ideas_this_week)` is an Array with size <= 5.
      - "show assigns @top_voted_ideas ordered by votes_count desc" — assert the assigned collection is sorted by `votes_count` descending (use `.map(&:votes_count)` and compare to its sorted reverse).
      - "show assigns @trending_ideas based on last-7-day votes" — seed (via fixture access or inline) a small number of recent votes on a known idea, assert that idea appears at the top of `assigns(:trending_ideas)`.
      - "trending metric uses account-scoped cache key" — assert `Rails.cache` was asked for key matching `account_#{account.id}/dashboard_trending/v1` (use `Rails.cache.fetch` interception via stub OR assert presence after the request: `assert Rails.cache.exist?("account_#{account.id}/dashboard_trending/v1")` — Solid Cache should honor `exist?`).
      - "ideas_this_week metric uses account-scoped cache key with v1 suffix" — same shape for the ideas_this_week key.
    All assertions use the existing `sign_in_as` helper from test_helper (per PATTERNS line 637) and `Current.account` resolved from the URL's account slug — review how existing admin controller tests set up the account context (likely `get admin_dashboard_url(script_name: @account.slug)` or via a `setup` block that sets `Current.account`).
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/dashboard_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Rails.cache.fetch(\"account_#{account.id}/dashboard_ideas_this_week/v1\"" app/controllers/admin/dashboard_controller.rb` returns at least one match (account-scoped key + v1 suffix per T-06-02).
    - `grep -F "Rails.cache.fetch(\"account_#{account.id}/dashboard_top_voted/v1\"" app/controllers/admin/dashboard_controller.rb` returns at least one match.
    - `grep -F "Rails.cache.fetch(\"account_#{account.id}/dashboard_trending/v1\"" app/controllers/admin/dashboard_controller.rb` returns at least one match.
    - `grep -F "expires_in: 10.minutes" app/controllers/admin/dashboard_controller.rb` returns at least 6 matches (3 existing + 3 new — confirm new ones are 10 minutes per D-04).
    - `grep -F "voteable_type: \"Idea\"" app/controllers/admin/dashboard_controller.rb` returns at least one match (the Trending guard per RESEARCH Pitfall 2).
    - `grep -F "Arel.sql(\"COUNT(*) DESC\")" app/controllers/admin/dashboard_controller.rb` returns at least one match (RESEARCH Pitfall 3 — Rails 7+ enforcement).
    - `grep -F ".to_a" app/controllers/admin/dashboard_controller.rb` returns at least 5 matches (existing 2 recent_* + 3 new — the .to_a is critical per RESEARCH Pattern 1 line 668).
    - `grep -F "next [] if ordered_idea_ids.empty?" app/controllers/admin/dashboard_controller.rb` returns one match (early return).
    - File `test/controllers/admin/dashboard_controller_test.rb` exists; `grep -c 'test "' test/controllers/admin/dashboard_controller_test.rb` returns >= 5 (the new tests plus any existing).
    - `git diff app/controllers/admin/dashboard_controller.rb | grep -E '^-.*@(stats|recent_ideas|recent_comments)' | wc -l` returns 0 (existing instance vars unchanged per D-01).
    - `bin/rails test test/controllers/admin/dashboard_controller_test.rb` exits 0.
  </acceptance_criteria>
  <done>Three new Rails.cache.fetch blocks added with account-scoped keys + Arel.sql trending query + .to_a + early-empty-return; controller test asserts assignment and cache-key shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Actionable section partials in dashboard view + add i18n keys</name>
  <files>app/views/admin/dashboard/show.html.erb, app/views/admin/dashboard/_actionable_section.html.erb, app/views/admin/dashboard/_actionable_tile.html.erb, app/assets/stylesheets/admin-dashboard.css, config/locales/en.yml</files>
  <read_first>
    - app/views/admin/dashboard/show.html.erb:1-89 (current full state — existing stat tile grid at lines 14-89, header lines 3-12; new section lands ABOVE line 14 per UI-SPEC line 274 + PATTERNS lines 405-411)
    - app/assets/stylesheets/panels.css:161+ (existing `.panel--stat` styles; reuse, do not re-implement per PATTERNS line 399)
    - app/assets/stylesheets/_global.css:13-22 (--space-* token scale per UI-SPEC §"Spacing Scale")
    - app/components/elements/empty_state_component.* (for the "No activity yet" inline empty per UI-SPEC line 107 + States Inventory line 261 — note: UI-SPEC says inline sub-line, NOT full EmptyStateComponent)
    - app/components/elements/avatar_component.* (for idea creator avatar in each tile row per UI-SPEC line 219)
    - app/components/elements/badge_component.* (for status badge per UI-SPEC line 218)
    - config/locales/en.yml (locate `admin.dashboard.show.stats.*` block; new `actionable.*` keys land as siblings under `admin.dashboard.show`)
    - .planning/phases/06-admin-notifications/06-UI-SPEC.md lines 97-108, 207-225, 239-262, 272-278 (copy contract + component inventory + states + layout ordering)
    - .planning/phases/06-admin-notifications/06-PATTERNS.md lines 368-413 (verbatim tile + grid + ordering pattern)
  </read_first>
  <behavior>
    - `app/views/admin/dashboard/show.html.erb` renders `_actionable_section` between the existing header (lines 3-12) and the existing stats grid (line 14 onward). The render line is a single `<%= render partial: "admin/dashboard/actionable_section" %>` insertion; no other lines in `show.html.erb` change (D-01 preserves the existing structure).
    - `_actionable_section.html.erb` contains: H2 heading from `t(".title")`, description paragraph from `t(".description")`, then a 3-column grid (1 col mobile, 3 cols at sm-breakpoint per UI-SPEC line 260) that renders `_actionable_tile` three times with different `metric:` and `ideas:` locals.
    - `_actionable_tile.html.erb` accepts `metric:` (Symbol — one of `:ideas_this_week`, `:top_voted`, `:trending`) and `ideas:` (Array<Idea>). It renders a `panel panel--stat` shell with the metric-specific label (via dynamic i18n key `t("admin.dashboard.show.actionable.#{metric}_label")`) and a 5-item idea list. Each row shows: idea title (link to `idea_path(idea)`), creator avatar (`Elements::AvatarComponent`), and vote count (`Elements::BadgeComponent`). When `ideas.empty?`, the tile body shows the inline empty subline `t(".empty_title")` + `t(".empty_description")` (UI-SPEC line 261 — preserve tile height; do NOT collapse).
    - The Trending tile additionally renders a sub-label `t(".trending_hint")` ("Votes in the last 7 days") tied to the tile via `aria-describedby` per UI-SPEC line 296.
    - i18n keys under `admin.dashboard.show.actionable.*` exist per UI-SPEC lines 99-108: `title`, `description`, `ideas_this_week_label`, `top_voted_label`, `trending_label`, `trending_hint`, `empty_title`, `empty_description`.
    - CSS additions, if any, live in `@layer modules` of `admin-dashboard.css` (or extend `panels.css` — choose whichever creates less new file overhead). UI-SPEC Spacing Scale (lines 34-42) restricts new spacing to the `--space-*` token set. No `!important`.
  </behavior>
  <action>
    Create `app/views/admin/dashboard/_actionable_section.html.erb` containing the H2 + description per UI-SPEC §"Copywriting Contract" lines 97-108 + a `<dl class="grid grid-cols-1 sm:grid-cols-3 gap-5">` (mirror the wrapper at app/views/admin/dashboard/show.html.erb:16). Inside the grid, render `_actionable_tile` three times with locals `metric: :ideas_this_week, ideas: @ideas_this_week`, `metric: :top_voted, ideas: @top_voted_ideas`, `metric: :trending, ideas: @trending_ideas`. Wrap the whole partial in a `<section class="margin-block-end-double">` (per UI-SPEC line 40: `--space-8` between Actionable and existing stats).

    Create `app/views/admin/dashboard/_actionable_tile.html.erb` accepting `metric:` and `ideas:`. Use the verbatim tile shell from PATTERNS lines 374-396 (app/views/admin/dashboard/show.html.erb:17-39): `<div class="panel panel--stat">` → `<div class="pad">` → flex header with optional icon (use lucide icons per UI-SPEC: `flame` for trending, `trending-up` for top_voted, `clock` for ideas_this_week — Claude's Discretion area; pick one icon per metric) + `<dl>` with `<dt class="panel__stat-label">` reading `t("admin.dashboard.show.actionable.#{metric}_label")` and `<dd>` containing a `<ul>` of up to 5 idea rows. Each `<li>` row contains: `link_to idea.title, idea_path(idea)`, `render Elements::AvatarComponent.new(user: idea.creator)`, `render Elements::BadgeComponent.new(variant: :secondary) { idea.votes_count.to_s }`. For the Trending tile, add a `<small class="txt-small txt-subtle" id="trending-hint"><%= t("admin.dashboard.show.actionable.trending_hint") %></small>` and add `aria-describedby="trending-hint"` to the outer `<div class="panel">` per UI-SPEC line 296. When `ideas.empty?`, render an inline `<p class="txt-small txt-subtle"><%= t(".empty_title") %></p>` + body per UI-SPEC line 261 — preserve the tile's outer panel structure (do NOT replace with EmptyStateComponent — that's only for the watchlist empty per UI-SPEC line 217).

    Modify `app/views/admin/dashboard/show.html.erb` with a SINGLE-LINE edit: insert `<%= render partial: "admin/dashboard/actionable_section" %>` between the existing header block (ends ~line 12) and the existing `<h2 class="txt-medium font-weight-bold margin-block-end">` of the stats grid (line 14-15). Do not touch any other line. Per D-01: existing tiles + recent activity lists remain untouched.

    Add i18n keys to `config/locales/en.yml` under `admin.dashboard.show.actionable:` (sibling to the existing `stats:` block) per UI-SPEC lines 99-108:
      - `title: "Actionable"`
      - `description: "What needs your attention this week"`
      - `ideas_this_week_label: "Ideas this week"`
      - `top_voted_label: "Top voted"`
      - `trending_label: "Trending"`
      - `trending_hint: "Votes in the last 7 days"`
      - `empty_title: "No activity yet"`
      - `empty_description: "Once people start submitting and voting, you'll see what to act on here."`

    If new CSS rules are needed (icon alignment in the tile row, gap between idea title and badge), add a NEW file `app/assets/stylesheets/admin-dashboard.css` wrapped in `@layer modules { .actionable-tile { ... } }` using `--space-*` tokens and OKLCH `--color-*` variables only (CLAUDE.md §"CSS Architecture"). Use logical properties (`padding-inline`, `margin-block-start`). Use BEM naming for any new selectors. If existing `.panel--stat` styles cover everything, skip the new CSS file entirely — RESEARCH Focus 8 line 491 + PATTERNS line 399 say no new CSS is required if `.panel--stat` is reused.

    Run `bin/i18n-tasks health` after the locale edit.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/dashboard_controller_test.rb && bin/i18n-tasks health && bin/erb_lint app/views/admin/dashboard/_actionable_section.html.erb app/views/admin/dashboard/_actionable_tile.html.erb app/views/admin/dashboard/show.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/views/admin/dashboard/_actionable_section.html.erb` exists; `grep -F "render partial: \"admin/dashboard/actionable_tile\"" app/views/admin/dashboard/_actionable_section.html.erb` returns 3 matches (one per metric tile).
    - File `app/views/admin/dashboard/_actionable_tile.html.erb` exists; `grep -F "panel--stat" app/views/admin/dashboard/_actionable_tile.html.erb` returns at least one match (reuses existing BEM shell per PATTERNS line 399).
    - `grep -F "render partial: \"admin/dashboard/actionable_section\"" app/views/admin/dashboard/show.html.erb` returns exactly one match.
    - The existing stats grid block at app/views/admin/dashboard/show.html.erb:14-89 is unchanged: `git diff app/views/admin/dashboard/show.html.erb | grep -E '^-.*panel--stat' | wc -l` returns 0 (only additions, no deletions of existing tile markup per D-01).
    - `grep -E '^\s+actionable:' config/locales/en.yml` returns at least one match (the new namespace).
    - `grep -F "Ideas this week" config/locales/en.yml` returns at least one match.
    - `grep -F "What needs your attention this week" config/locales/en.yml` returns one match.
    - `grep -F "trending_hint:" config/locales/en.yml` returns one match.
    - `grep -F "aria-describedby" app/views/admin/dashboard/_actionable_tile.html.erb` returns at least one match (UI-SPEC line 296 accessibility contract for trending tile).
    - `bin/i18n-tasks health` exits 0.
    - `bin/erb_lint --lint-all` for the three view files exits 0 (or surfaces only existing offenses).
    - `bin/rails test test/controllers/admin/dashboard_controller_test.rb` still exits 0 (view render does not break the controller test).
  </acceptance_criteria>
  <done>Dashboard renders Actionable section above existing stats with 3 metric tiles; i18n keys complete; existing tiles untouched per D-01; ERB lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP GET /admin/dashboard → controller | Authenticated admin request (ApplicationController auth gate + role guard); per-request `Current.account` set by AccountSlug::Extractor middleware (CLAUDE.md §"Controller Structure") |
| Controller → Rails.cache (Solid Cache) | Cache backend is SQLite (Solid Cache); keys MUST include account scope (T-06-02) |
| Cache → JSON serialization | ActiveRecord objects serialized with their eager-loaded associations; `.to_a` is mandatory (RESEARCH Pattern 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Rails.cache.fetch keys for the 3 new dashboard metrics | mitigate | All three cache keys include `account_#{account.id}/` prefix per RESEARCH Pitfall 6 + PATTERNS line 896. Acceptance criterion grep enforces the prefix. Test "trending metric uses account-scoped cache key" asserts the key shape. Multi-tenant isolation tests from Phase 1 FOUND-02 also catch cross-account cache poisoning (CLAUDE.md §"Authentication & Authorization"). |
| T-06-DA-01 | Tampering | SQL injection via `order(...)` | mitigate | The Trending aggregation uses `Arel.sql("COUNT(*) DESC")` with a CONSTANT string (no user input interpolation). RESEARCH Pitfall 3 + Security Domain table (RESEARCH line 1127). Acceptance criterion grep enforces `Arel.sql("COUNT(*) DESC")`. |
| T-06-DA-02 | Information Disclosure | Trending mixing Idea + Comment voteable IDs | mitigate | `voteable_type: "Idea"` filter mandatory per RESEARCH Pitfall 2. Acceptance criterion grep enforces the filter. Without this guard, a Comment ID could shadow an Idea ID in the trending list. |
| T-06-DA-03 | Denial of Service | Uncached query under load | mitigate | All three metrics wrapped in `Rails.cache.fetch` with 10-min TTL (D-04). Even on cache miss the queries are bounded (`limit(5)` + indexed columns); RESEARCH Focus 1 line 130 confirms scan is in-memory at v1 seed scale. |
</threat_model>

<verification>
After both tasks complete:
- `bin/rails test test/controllers/admin/dashboard_controller_test.rb` exits 0 with at least 5 new tests reporting pass.
- `bin/rails test` (full Minitest, no system) exits 0 — controller extension must not break any other test.
- `bin/erb_lint --lint-all` exits 0 (or only flags pre-existing offenses).
- `bin/i18n-tasks health` exits 0.
- `bin/rubocop app/controllers/admin/dashboard_controller.rb test/controllers/admin/dashboard_controller_test.rb` exits 0.
- Manual (per VALIDATION.md line 75): open `/<account>/admin/dashboard`; confirm new "Actionable" section above the existing stats row, three tiles each rendering up to 5 items.
</verification>

<success_criteria>
- ADMN-01 ships: Actionable section visible above existing stats row (D-01) with 3 cached metric lists (D-02 Trending = votes in last 7 days; D-03 = 5 items each; D-04 = 10-minute TTL).
- Cache keys are account-scoped (T-06-02 mitigated).
- No new database schema (D-05 confirmed — zero migrations).
- Existing stats tiles + recent activity grid remain unchanged.
- ERB lints clean; i18n health clean; Rubocop clean.
</success_criteria>

<output>
Create `.planning/phases/06-admin-notifications/06-03-SUMMARY.md` when done.
</output>
