<%= turbo_stream.update "flash_toast", partial: "flash_toast" %>

<%= turbo_stream.remove "comment_#{@comment.id}" %>

<%= turbo_stream.update "idea_#{@comment.idea.id}_comment_count" do %>
  <%= t('comments.comments.comments_title_with_count', comment_count: @comment.idea.comments_count) %>
<% end %>
