<% content_for(:title) { t(".title") } %>

<div class="container">
  <div class="margin-block-start-double">
    <div class="panel" style="padding: var(--space-6);">
      <div class="flex flex-column gap-6">
        <div>
          <h2 class="txt-large font-weight-medium"><%= t(".title") %></h2>
          <p class="txt-small txt-subtle"><%= t(".body") %></p>
        </div>

        <div>
          <%= link_to t(".cta"), sign_in_path, class: "btn btn--primary" %>
        </div>
      </div>
    </div>
  </div>
</div>
