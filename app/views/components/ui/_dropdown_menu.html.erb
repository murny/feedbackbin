<el-dropdown class="inline-block">
  <%= render_button(**trigger_button_options.merge(variant: trigger_button_variant)) do %>
    <%= trigger %>
  <% end %>

  <el-menu anchor="bottom start" popover class="<%= content_options[:class] %>">
    <%= content %>
  </el-menu>
</el-dropdown>
