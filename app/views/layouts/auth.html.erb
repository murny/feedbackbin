<!DOCTYPE html>
<html lang="en">
  <%= render "shared/head" %>

  <%= tag.body class: "flex flex-column",
        style: "min-block-size: 100svh;",
        data: {
          controller: "local-time theme-application",
          theme_application_preference_value: "system"
        } do %>
    <%= render "flash" %>
    <main class="flex-item-grow flex flex-column align-center justify-center gap" style="max-inline-size: none; padding: var(--space-6); --row-gap: var(--space-6);">
      <%= yield %>
    </main>
  <% end %>
</html>
