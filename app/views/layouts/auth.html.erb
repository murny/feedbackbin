<!DOCTYPE html>
<html class="h-full bg-background overscroll-none font-sans antialiased">
  <%= render "shared/head" %>

  <%= tag.body class: "h-full flex flex-col",
        data: {
          controller: "local-time theme-application",
          theme_application_preference_value: "system"
        } do %>
    <%= render "flash" %>
    <main class="flex-1 flex flex-col items-center justify-center gap-6 p-6 md:p-10">
      <%= yield %>
    </main>
  <% end %>
</html>
