<div id="button-expanded">
  <% content_for :preview_example, flush: true do %>
      <%= render_button(text: "Expanded button", class: "w-full justify-center") %>
  <% end %>

  <% content_for :code_example, flush: true do %>
<%%= render_button(text: "Expanded button", class: "w-full justify-center") %>
  <% end %>

  <%= render partial: "feedbackbin_elements/docs/shared/code_example", locals: {
    heading: "Expanded display",
    description: "If you need a button to span fullwidth add the appropriate Tailwind CSS width and margin classes to existing button classes."
  } %>
</div>
