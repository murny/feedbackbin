<div id="card-layouts">
  <% content_for :preview_example, flush: true do %>
    <div class="flex w-full flex-wrap items-start gap-8 md:*:data-[slot=card]:basis-1/4">
      <%= render_card(
        body: "Content only card with no header or footer."
      ) %>

      <%= render_card(
        title: "Header Only",
        description: "This card has just a header with title and description."
      ) %>

      <%= render_card(
        title: "Header & Content",
        description: "This card has a header and content.",
        body: "This is the card content area."
      ) %>

      <%= render_card(
        footer: "Footer only card"
      ) %>

      <%= render_card(
        title: "Header + Footer Only",
        description: "This is a card with a header and a footer.",
        footer: "Footer"
      ) %>

      <%= render_card(
        body: "Card with content and footer.",
        footer: "Footer content"
      ) %>

      <%= render_card(
        title: "Complete Card",
        description: "This card has header, content and footer.",
        body: "This is the main content area.",
        footer: "Card footer"
      ) %>
    </div>
  <% end %>

  <% content_for :code_example, flush: true do %>
<%%= render_card(
  body: "Content only card with no header or footer."
) %>

<%%= render_card(
  title: "Header Only",
  description: "This card has just a header with title and description."
) %>

<%%= render_card(
  title: "Header & Content",
  description: "This card has a header and content.",
  body: "This is the card content area."
) %>

<%%= render_card(
  footer: "Footer only card"
) %>

<%%= render_card(
  title: "Header + Footer Only",
  description: "This is a card with a header and a footer.",
  footer: "Footer"
) %>

<%%= render_card(
  body: "Card with content and footer.",
  footer: "Footer content"
) %>

<%%= render_card(
  title: "Complete Card",
  description: "This card has header, content and footer.",
  body: "This is the main content area.",
  footer: "Card footer"
) %>
  <% end %>

  <%= render partial: "feedbackbin_elements/docs/shared/code_example", locals: { heading: "Card Layouts" } %>
</div>
